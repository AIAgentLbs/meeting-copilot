name: Publish v0.1.4 meeting history by day

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/release-0.1.4.yml

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test meeting history UI
        run: |
          python3 copilot/web/server.py --self-test
          node --check copilot/web/app.js
      - name: Patch the existing macOS package
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/base" "$RUNNER_TEMP/stage"
          gh release download v0.1.3 --pattern meeting-copilot-macos.zip --dir "$RUNNER_TEMP/base"
          unzip -q "$RUNNER_TEMP/base/meeting-copilot-macos.zip" -d "$RUNNER_TEMP/stage"
          test -f "$RUNNER_TEMP/stage/payload/web/app.js"
          cp copilot/web/app.js "$RUNNER_TEMP/stage/payload/web/app.js"
          cp copilot/web/index.html "$RUNNER_TEMP/stage/payload/web/index.html"
          cp copilot/web/styles.css "$RUNNER_TEMP/stage/payload/web/styles.css"
          (cd "$RUNNER_TEMP/stage" && zip -qry "$RUNNER_TEMP/meeting-copilot-macos.zip" .)
          unzip -tq "$RUNNER_TEMP/meeting-copilot-macos.zip"
          shasum -a 256 "$RUNNER_TEMP/meeting-copilot-macos.zip"
      - name: Publish v0.1.4
        env:
          GH_TOKEN: ${{ github.token }}
        run: >-
          gh release create v0.1.4 "$RUNNER_TEMP/meeting-copilot-macos.zip"
          --target "$GITHUB_SHA"
          --title "Meeting Copilot v0.1.4"
          --notes "Makes meeting history easy to find and groups local recordings by calendar day in Russian and English. The native capture binary is unchanged."
